# まなびの基板 — セットアップ

所要 15分ほど。上から順にやれば動きます。

---

## 1. スプレッドシートを作る

1. Googleドライブで新しいスプレッドシートを作り、名前を「まなびの基板」などにする
2. 上のメニューから **拡張機能 → Apps Script** を開く
3. 出てきたエディタの中身をぜんぶ消して、`code.gs` の中身を貼りつける
4. 先頭にある2つの合言葉を、自分で決めた文字列に書きかえる

```js
var WRITE_TOKEN = 'manabi-write-2026';   // 生徒アプリが使う。公開されます
var ADMIN_TOKEN = 'manabi-admin-2026';   // 先生だけが使う。生徒に見せない
```

5. 保存する（ディスクのアイコン）

---

## 2. ウェブアプリとして公開する

1. 右上の **デプロイ → 新しいデプロイ**
2. 歯車マークから **ウェブアプリ** を選ぶ
3. 設定を次のようにする

| 項目 | 選ぶもの |
|---|---|
| 次のユーザーとして実行 | 自分 |
| アクセスできるユーザー | **全員** |

4. デプロイを押し、アクセス許可を求められたら承認する
5. 出てきた `https://script.google.com/macros/s/.../exec` をコピーしておく

> **ここでつまずいたら**
> 学校・自治体のアカウントでは「全員」が選べないことがあります。
> その場合は個人のGoogleアカウントで同じ手順をやり直すと通ります。
> URLの末尾に `?mode=ping` を付けてブラウザで開き、`つながっています` と出れば成功です。

コードを直したときは、毎回 **デプロイ → デプロイを管理 → 鉛筆マーク → バージョン「新バージョン」→ デプロイ** が必要です。ここを忘れると古いままです。

---

## 3. 生徒用アプリにつなぐ

`config.js` を開いて2か所を書きかえます。

```js
window.MANABI_CONFIG = {
  endpoint: 'ここに手順2でコピーしたURL',
  token: 'WRITE_TOKEN と同じ文字列',
  rankLimit: 20
};
```

これだけで、記録の送信とランキング表示の両方が動きます。

---

## 4. 各ゲームに1行足す

クリア時・ゲームオーバー時に、次を呼びます。

```html
<script src="config.js"></script>
<script src="hub-progress.js"></script>
<script>
  LearnHub.report({
    app:  'english-go',   // アプリの識別キー
    done: 3,              // 到達したステージ数
    total: 4,             // 全ステージ数
    best: 82,             // 得点や正答率（0〜100）
    note: 'S3クリア'      // 一覧に出るひとこと（省略可）
  });
</script>
```

`app` に入れる文字列：

| アプリ | キー |
|---|---|
| 電気分野 3D LAB | `electricity` |
| 英語でGO! | `english-go` |
| 熟語の迷宮 | `idiom-dungeon` |
| 日本一周クエスト | `japan-quest` |
| サイエンスクエスト | `science-quest` |

通信が切れていても記録は端末に貯まり、次にアプリを開いたときにまとめて送られます。

---

## 5. 先生用の計器盤

`dashboard.html` をブラウザで開き、手順2のURLと `ADMIN_TOKEN` を入れて「よみこむ」。
入力した値はその端末にだけ残ります。

**この2ファイルは生徒に配らないでください**
- `dashboard.html`（先生用画面）
- スプレッドシート本体の共有リンク

`dashboard.html` をGitHub Pagesに置くと生徒からもURLが見えます。合言葉がなければ中身は開けませんが、先生の端末に置いてローカルで開くほうが安全です。

---

## ファイル一覧

| ファイル | 置き場所 | 役割 |
|---|---|---|
| `index.html` | 公開する | ハブ画面 |
| `config.js` | 公開する | 接続先の設定 |
| `hub-progress.js` | 公開する | 記録の保存と送信 |
| `manifest.json` `icon-192.png` `icon-512.png` | 公開する | ホーム画面アイコン用 |
| 各ゲームのHTML | 公開する | 教材本体 |
| `code.gs` | Apps Script | サーバー側 |
| `dashboard.html` | 先生の端末 | 集計とグラフ |

---

## 気をつけること

- **本名は入れない。** ニックネームだけを送る作りにしてあります。公開サイトに載るという前提で運用してください
- **合言葉は完全な鍵ではありません。** `config.js` に書く `WRITE_TOKEN` は誰でも読めます。いたずらで嘘の記録が入ったら、スプレッドシートの `records` と `best` から該当行を消せば直ります
- **消えては困る記録は、ときどきスプレッドシートを複製して残しておく**
- 端末のブラウザの履歴を消すと、その端末に残っていた進捗も消えます。スプレッドシート側の記録は残ります
