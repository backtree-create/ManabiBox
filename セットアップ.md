# まなびの基板 — セットアップ

所要 15分ほど。上から順にやれば動きます。
スプレッドシートにつながなくても、ハブ画面とゲームの起動、端末ごとの進捗表示までは動きます。

---

## 1. スプレッドシートを作る

1. Googleドライブで新しいスプレッドシートを作り、名前を「まなびの基板」などにする
2. 上のメニューから **拡張機能 → Apps Script** を開く
3. エディタの中身をぜんぶ消して、`code.gs` の中身を貼りつける
4. 先頭にある2つの合言葉を、自分で決めた文字列に書きかえる

```js
var WRITE_TOKEN = 'manabi-write-2026';   // 生徒アプリが使う。公開されます
var ADMIN_TOKEN = 'manabi-admin-2026';   // 先生だけが使う。生徒に見せない
```

5. 保存する

---

## 2. ウェブアプリとして公開する

1. 右上の **デプロイ → 新しいデプロイ**
2. 歯車マークから **ウェブアプリ** を選ぶ
3. 設定を次のようにする

| 項目 | 選ぶもの |
|---|---|
| 次のユーザーとして実行 | 自分 |
| アクセスできるユーザー | **全員** |

4. デプロイを押し、アクセス許可を求められたら承認する
5. 出てきた `https://script.google.com/macros/s/.../exec` をコピーしておく

> **ここでつまずいたら**
> 学校・自治体のアカウントでは「全員」が選べないことがあります。
> その場合は個人のGoogleアカウントで同じ手順をやり直すと通ります。
> URLの末尾に `?mode=ping` を付けてブラウザで開き、`つながっています` と出れば成功です。

コードを直したら、毎回 **デプロイ → デプロイを管理 → 鉛筆マーク → バージョン「新バージョン」→ デプロイ** が必要です。ここを忘れると古いままです。

---

## 3. 生徒用アプリにつなぐ

`config.js` の2か所を書きかえます。

```js
window.MANABI_CONFIG = {
  endpoint: 'ここに手順2でコピーしたURL',
  token: 'WRITE_TOKEN と同じ文字列',
  rankLimit: 20
};
```

これで、記録の送信とランキング表示の両方が動きます。

---

## 4. 各ゲームに1行足す

クリア時・ゲームオーバー時に、次を呼びます。

```html
<script src="config.js"></script>
<script src="hub-progress.js"></script>
<script>
  LearnHub.report({
    app:  'english-go',   // 識別キー
    done: 3,              // 到達したステージ数
    total: 4,             // 全ステージ数
    best: 82,             // 得点や正答率（0〜100）
    note: 'S3クリア'      // 一覧に出るひとこと（省略可）
  });
</script>
```

通信が切れていても記録は端末に貯まり、次に開いたときにまとめて送られます。

---

## 5. あとからゲームを増やす

**`manage.html` を開いて登録するだけです。** コードは触りません。

1. 新しいゲームのHTMLを、`index.html` と同じ場所に置く
2. `manage.html` を開く（ハブ画面いちばん下の「ゲームを追加する」からも行けます）
3. 先生用の合言葉を入れる
4. フォームに入れる

| 項目 | 例 | 意味 |
|---|---|---|
| ゲーム名 | 漢字マスター | カードの見出し |
| 教科 | 国語 | カード上部の小さなラベル |
| ファイル名 | `kanji.html` | 押したときに開くファイル |
| 識別キー | `kanji-master` | `report({app:'...'})` に書く文字列。半角英数とハイフン |
| せつめい | ひと言 | カードの本文 |
| 単位 / ぜんぶで何個 | ステージ / 10 | 進み具合の数え方 |
| 色 / アイコン | 選ぶだけ | カードの色とマーク |

5. 「一覧に入れる」→「スプレッドシートに保存」
6. 新しいゲーム側にも、手順4の `LearnHub.report()` を入れる

生徒の画面は次に開いたときに新しい一覧になります。並べかえ、修正、削除も同じ画面からできます。

**スプレッドシートを使わない場合**は、「apps.json を書き出す」で保存したファイルを、リポジトリの `apps.json` と差しかえてください。`apps.json` を直接テキストで編集しても同じです。

読み込みは **スプレッドシート → `apps.json` → 前回の控え** の順に試すので、どれか1つが生きていれば画面は出ます。

---

## 6. 先生用の記録画面

`dashboard.html` をブラウザで開き、手順2のURLと `ADMIN_TOKEN` を入れて「よみこむ」。
入力した値はその端末にだけ残ります。

**この2つは生徒に配らないでください**
- `ADMIN_TOKEN`
- スプレッドシート本体の共有リンク

`dashboard.html` と `manage.html` は、合言葉がなければ中身を読めませんが、先生の端末に置いてローカルで開くほうが安全です。

---

## ファイル一覧

| ファイル | 置き場所 | 役割 |
|---|---|---|
| `index.html` | 公開する | ハブ画面 |
| `theme.css` | 公開する | 見た目 |
| `icons.js` | 公開する | アイコン |
| `config.js` | 公開する | 接続先の設定 |
| `hub-progress.js` | 公開する | 記録の保存と送信 |
| `apps.json` | 公開する | ゲーム一覧の控え |
| `manifest.json` `icon-192.png` `icon-512.png` | 公開する | ホーム画面アイコン用 |
| 各ゲームのHTML | 公開する | 教材本体 |
| `code.gs` | Apps Script | サーバー側 |
| `manage.html` | 先生の端末 | ゲームの追加と並べかえ |
| `dashboard.html` | 先生の端末 | 集計とグラフ |

---

## 気をつけること

- **本名は入れない。** ニックネームだけを送る作りです。公開サイトに載る前提で運用してください
- **合言葉は完全な鍵ではありません。** `config.js` の `WRITE_TOKEN` は誰でも読めます。嘘の記録が入ったら、スプレッドシートの `records` と `best` から該当行を消せば直ります
- **消えては困る記録は、ときどきスプレッドシートを複製して残す**
- 端末のブラウザ履歴を消すと、その端末の進捗も消えます。スプレッドシート側の記録は残ります
